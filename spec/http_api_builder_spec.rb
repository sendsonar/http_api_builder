require 'spec_helper'

describe HttpApiBuilder do
  it 'has a version number' do
    expect(HttpApiBuilder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
